---
// Root URL → BG landing rendered directly (no redirect).
// Avoids the Safari iOS "Load cannot follow more than 20 redirections" loop
// that the previous meta-refresh page produced.
import Landing from "../components/Landing.astro";
---
<Landing lang="bg" />
